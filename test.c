#include <stdio.h>

const int MAX = 10;

int main() {
    // int i;
    // for (i = 1; i <= 10; i++)
    // {
    //     printf("%f \n", 1/i);
    // }

    // printf("\xFF");

    char c;
    while ((c = getchar()) != EOF) {
        putchar(c);
    }
    
    return 0;
}